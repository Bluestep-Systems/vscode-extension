import * as vscode from 'vscode';
import { State } from '../../';
/**
 * TODO
 */
export default function(): void {
  vscode.window.showInformationMessage('Hello World from bsjs-push-pull.pushScript!');
}